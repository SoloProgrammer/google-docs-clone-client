export const SERVER_URL = import.meta.env.VITE_SERVER_URL;

export const LOGIN_URI = `${SERVER_URL}/api/auth/callback/google`;

export const SESSION_URI = `${SERVER_URL}/api/auth/session`;

export const DOCUMENT_URI = `${SERVER_URL}/api/documents`